:orphan:

Visualization
=============

SAP Warp separates simulation from visualization. Simulation entry points such
as ``benchmark.py`` are intended for timing, validation, and batch runs. Viewer
entry points are optional tools that consume the same scene files and runtime
objects for inspection.

Current viewer options:

:doc:`viser`

Additional viewer backends can be added under this section without changing
the simulation documentation.
